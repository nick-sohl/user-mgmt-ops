apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: user-mgmt-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/nick-sohl/user-mgmt-ops.git
    targetRevision: main
    path: helm/user-mgmt
    helm:
      releaseName: user-mgmt-prod
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: user-mgmt-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
